; Upload limits (UpdraftPlus / media / plugins)
upload_max_filesize = 1024M
post_max_size = 1024M

; Execution limits (backup + restore)
max_execution_time = 600
max_input_time = 600

; Memory (PHP, not WP)
memory_limit = 512M
